import { BaseBackTesting } from './base-back-testing';

/**
 * @class HasPendingOrders
 * @summary Retorna verdadeiro (True) caso existam ordens pendentes.
 */
export class HasPendingOrders extends BaseBackTesting {
  constructor() {
    super();
  }
}
