import { BaseBackTesting } from './base-back-testing';

/**
 * @class Price
 * @summary Retorna o preço de compra ou venda da posição, dependendo se estiver comprado ou vendido.
 */
export class Price extends BaseBackTesting {
  constructor() {
    super();
  }
}
