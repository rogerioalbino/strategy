import { BaseBackTesting } from './base-back-testing';

/**
 * @class Position
 * @summary Retorna o tamanho da posição, positivo para compra e negativo para venda.
 */
export class Position extends BaseBackTesting {
  constructor() {
    super();
  }
}
