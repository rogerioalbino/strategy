import { BaseIndicator } from './base-indicator';

/**
 * @class Envelope
 * @summary Retorna o valor do indicador Envelope.
 */
export class Envelope extends BaseIndicator {
  constructor() {
    super();
  }
}
