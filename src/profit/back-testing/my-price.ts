import { BaseBackTesting } from './base-back-testing';

/**
 * @class MyPrice
 * @summary Retorna a média entre a máxima, mínima e fechamento.
 */
export class MyPrice extends BaseBackTesting {
  constructor() {
    super();
  }
}
