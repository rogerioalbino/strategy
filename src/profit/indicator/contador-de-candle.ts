import { BaseIndicator } from './base-indicator';

/**
 * @class ContadorDeCandle
 * @summary Contabiliza e sinaliza de forma numérica e organizada no gráfico o número de cada candle.
 */
export class ContadorDeCandle extends BaseIndicator {
  constructor() {
    super();
  }
}
