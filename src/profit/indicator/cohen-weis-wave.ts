import { BaseIndicator } from './base-indicator';

/**
 * @class CohenWeisWave
 * @summary Desenvolvido por Rodrigo Cohen e equipe, indica a soma do volume de cada swing do ativo. Serve como um indicador de exaustão.
 */
export class CohenWeisWave extends BaseIndicator {
  constructor() {
    super();
  }
}
