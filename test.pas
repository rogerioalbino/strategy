// Strategy
  begin
  // Entries
    // BuyAtMarket  
      // Indicator
        if sma > priceNow then
          BuyAtMarket
      // Indicator
        if sma > priceNow then
          BuyAtMarket

    // BuyAtLimit  
      // Indicator
        if sma > priceNow then
          BuyAtLimit
        
  // Exits
    // SellAtMarket  
      // Indicators
      if sma > priceNow then
        SellAtMarket
        
      if sma > priceNow then
        SellAtMarket

    // SellAtLimit  
      // Indicators
      if sma > priceNow then
        SellAtLimit
  end;