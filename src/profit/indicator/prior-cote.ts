import { BaseIndicator } from './base-indicator';

/**
 * @class PriorCote
 * @summary Retorna o valor do indicador Prior Cote, de acordo com o dado desejado.
 */
export class PriorCote extends BaseIndicator {
  constructor() {
    super();
  }
}
