import { BaseIndicator } from './base-indicator';

/**
 * @class DidiIndex
 * @summary Retorna o valor do indicador DI+/DI- de acordo com a linha desejada.
 */
export class DidiIndex extends BaseIndicator {
  constructor() {
    super();
  }
}
