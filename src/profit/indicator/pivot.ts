import { BaseIndicator } from './base-indicator';

/**
 * @class Pivot
 * @summary Retorna o valor do indicador Pivot, de acordo com a linha selecionada.
 */
export class Pivot extends BaseIndicator {
  constructor() {
    super();
  }
}
