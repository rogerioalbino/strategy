import { BaseIndicator } from './base-indicator';

/**
 * @class VWAPDate
 * @summary Retorna o preço médio ponderado pelo volume, a partir de uma data e horário específicos.
 */
export class VWAPDate extends BaseIndicator {
  constructor() {
    super();
  }
}
