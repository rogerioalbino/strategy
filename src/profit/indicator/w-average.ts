import { BaseIndicator } from './base-indicator';

/**
 * @class WAverage
 * @summary Retorna o dado do indicador Média Móvel(Ponderada).
 */
export class WAverage extends BaseIndicator {
  constructor() {
    super();
  }
}
