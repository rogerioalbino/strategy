import { BaseBackTesting } from './base-back-testing';

/**
 * @class BuyPrice
 * @summary Retorna o preço de compra da posição.
 */
export class BuyPrice extends BaseBackTesting {
  constructor() {
    super();
  }
}
