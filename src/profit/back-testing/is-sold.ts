import { BaseBackTesting } from './base-back-testing';

/**
 * @class IsSold
 * @summary Retorna verdadeiro (True) caso exista posição venda.
 */
export class IsSold extends BaseBackTesting {
  constructor() {
    super();
  }
}
