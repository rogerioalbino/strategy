import { BaseIndicator } from './base-indicator';

/**
 * @class HSI
 * @summary Retorna o valor do indicador IFR Índice de Força Harmônico (HSI).
 */
export class HSI extends BaseIndicator {
  constructor() {
    super();
  }
}
