import { BaseBackTesting } from './base-back-testing';

/**
 * @class IsBought
 * @summary Retorna verdadeiro (True) caso exista posição de compra.
 */
export class IsBought extends BaseBackTesting {
  constructor() {
    super();
  }
}
