import { BaseBackTesting } from './base-back-testing';

/**
 * @class SellPrice
 * @summary Retorna o preço de venda da posição.
 */
export class SellPrice extends BaseBackTesting {
  constructor() {
    super();
  }
}
