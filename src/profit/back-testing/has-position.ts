import { BaseBackTesting } from './base-back-testing';

/**
 * @class HasPosition
 * @summary Retorna verdadeiro se a posição não é zero.
 */
export class HasPosition extends BaseBackTesting {
  constructor() {
    super();
  }
}
