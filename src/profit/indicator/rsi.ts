import { BaseIndicator } from './base-indicator';

/**
 * @class RSI
 * @summary Retorna o valor do indicador IFR(RSI).
 */
export class RSI extends BaseIndicator {
  constructor() {
    super();
  }
}
