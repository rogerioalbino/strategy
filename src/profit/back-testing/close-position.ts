import { BaseBackTesting } from './base-back-testing';

/**
 * @class ClosePosition
 * @summary Envia ordens para encerrar todas as posições.
 */
export class ClosePosition extends BaseBackTesting {
  constructor() {
    super();
  }
}
