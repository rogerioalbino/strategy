import { BaseBackTesting } from './base-back-testing';

/**
 * @class ReversePosition
 * @summary Envia ordens para inverter a posição.
 */
export class ReversePosition extends BaseBackTesting {
  constructor() {
    super();
  }
}
