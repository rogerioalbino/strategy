import { BaseIndicator } from './base-indicator';

/**
 * @class CohenPriceWave
 * @summary Desenvolvido por Rodrigo Cohen e equipe, indica a diferença absoluta de preços de cada swing do ativo.
 */
export class CohenPriceWave extends BaseIndicator {
  constructor() {
    super();
  }
}
