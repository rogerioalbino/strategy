import { BaseIndicator } from './base-indicator';

/**
 * @class Media
 * @summary Retorna o dado do indicador Média Móvel(Aritmética).
 */
export class Media extends BaseIndicator {
  constructor() {
    super();
  }
}
