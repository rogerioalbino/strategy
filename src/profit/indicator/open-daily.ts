import { BaseIndicator } from './base-indicator';

/**
 * @class OpenDaily
 * @summary Retorna o dado de abertura, conforme o deslocamento especificado.
 */
export class OpenDaily extends BaseIndicator {
  constructor() {
    super();
  }
}
