import { BaseBackTesting } from './base-back-testing';

/**
 * @class BuyPosition
 * @summary Retorna o tamanho da posição da compra.
 */
export class BuyPosition extends BaseBackTesting {
  constructor() {
    super();
  }
}
