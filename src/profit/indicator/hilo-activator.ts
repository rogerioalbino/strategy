import { BaseIndicator } from './base-indicator';

/**
 * @class HiloActivator
 * @summary Retorna o valor do HiLo Activator de acordo com a linha desejada.
 */
export class HiloActivator extends BaseIndicator {
  constructor() {
    super();
  }
}
