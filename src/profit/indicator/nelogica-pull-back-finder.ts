import { BaseIndicator } from './base-indicator';

/**
 * @class NelogicaPullBackFinder
 * @summary Retorna o valor do indicador Nelogica PullBack Finder de acordo com a linha desejada.
 */
export class NelogicaPullBackFinder extends BaseIndicator {
  constructor() {
    super();
  }
}
